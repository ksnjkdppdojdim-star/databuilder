<div class="test-wrapper">
    <p>ROOT TEMPLATE EXECUTING</p>
    <?php 
    $header = $block->getChild('header');
    if ($header) {
        echo $header->render();
    } else {
        echo '<p>ERROR: header block not found</p>';
    }
    ?>
    <?php 
    $content = $block->getChild('content');
    if ($content) {
        echo $content->render();
    } else {
        echo '<p>ERROR: content block not found</p>';
    }
    ?>
    <?php 
    $footer = $block->getChild('footer');
    if ($footer) {
        echo $footer->render();
    } else {
        echo '<p>ERROR: footer block not found</p>';
    }
    ?>
    <p>END ROOT TEMPLATE</p>
</div>
