<?php
/**
 * Admin > Layout — Autres paramètres (labels du menu principal)
 *
 * @var \DataBuilder\Block\ContainerBlock $block
 */
$trTitle      = $block->getData('tr_other_settings',        'Other settings');
$trShowLabels = $block->getData('tr_main_menu_show_labels', 'Show labels for main menu links');
$trEnabled    = $block->getData('tr_enabled',               'Enabled');
$trDisabled   = $block->getData('tr_disabled',              'Disabled');
$trUpdate     = $block->getData('tr_update',                'Update');
$labelsOn     = $block->getData('show_labels_on',  '');
$labelsOff    = $block->getData('show_labels_off', ' checked');

$esc = fn(string $s): string => htmlspecialchars($s, ENT_QUOTES, 'UTF-8');
?>
<div class="db-card">
    <div class="db-card-header">
        <h2><?= $esc($trTitle) ?></h2>
    </div>
    <div class="db-card-body">
        <form class="db-form" method="post" action="layout.php">
            <div class="db-form-row db-form-row--radio">
                <label><?= $esc($trShowLabels) ?></label>
                <div class="db-radio-group">
                    <label class="db-radio-label">
                        <input type="radio" name="mainMenuShowLabels"
                               id="mainMenuShowLabels_enabled"
                               value="1"<?= $labelsOn ?>>
                        <?= $esc($trEnabled) ?>
                    </label>
                    <label class="db-radio-label">
                        <input type="radio" name="mainMenuShowLabels"
                               id="mainMenuShowLabels_disabled"
                               value="0"<?= $labelsOff ?>>
                        <?= $esc($trDisabled) ?>
                    </label>
                </div>
            </div>

            <div class="db-form-actions">
                <input type="hidden" name="uaction" value="changeShowLabels">
                <button type="submit" class="db-btn db-btn-primary">
                    <?= $esc($trUpdate) ?>
                </button>
            </div>
        </form>
    </div>
</div>
