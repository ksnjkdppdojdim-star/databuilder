<?php
/**
 * DataBuilder Admin Index Template
 * 
 * This template renders the admin dashboard with statistics cards
 * Uses DataBuilder blocks system for rendering
 */

// Get the DataBuilder data for this page
$pageData = $this->getPageData() ?? [];

// Extract variables for easy access
$adminUsers = $this->getData('ADMIN_USERS', '0');
$trAdminUsers = $this->getData('TR_ADMIN_USERS', 'Admin Users');

$resellerUsers = $this->getData('RESELLER_USERS', '0');
$trResellerUsers = $this->getData('TR_RESELLER_USERS', 'Reseller Users');

$normalUsers = $this->getData('NORMAL_USERS', '0');
$trNormalUsers = $this->getData('TR_NORMAL_USERS', 'Normal Users');

$domains = $this->getData('DOMAINS', '0');
$trDomains = $this->getData('TR_DOMAINS', 'Domains');

$ftpAccounts = $this->getData('FTP_ACCOUNTS', '0');
$trFtpAccounts = $this->getData('TR_FTP_ACCOUNTS', 'FTP Accounts');

$sqlDatabases = $this->getData('SQL_DATABASES', '0');
$trSqlDatabases = $this->getData('TR_SQL_DATABASES', 'SQL Databases');

$sqlUsers = $this->getData('SQL_USERS', '0');
$trSqlUsers = $this->getData('TR_SQL_USERS', 'SQL Users');

$mailAccounts = $this->getData('MAIL_ACCOUNTS', '0');
$trMailAccounts = $this->getData('TR_MAIL_ACCOUNTS', 'Mail Accounts');

$trServerTraffic = $this->getData('TR_SERVER_TRAFFIC', 'Server Traffic');
$trafficWarning = $this->getData('TRAFFIC_WARNING', '');
$trafficPercentWidth = $this->getData('TRAFFIC_PERCENT_WIDTH', '0');
$trafficPercent = $this->getData('TRAFFIC_PERCENT', '0');
?>

<div class="space-y-8">
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-6 gap-6">
        <!-- Admin Users Card -->
        <div class="bg-white dark:bg-[#1a222f] p-6 rounded-xl border border-slate-200 dark:border-slate-800 hover:border-primary/40 transition-all shadow-sm lg:col-span-2">
            <div class="flex items-center justify-between">
                <div>
                    <div class="text-3xl font-black text-slate-900 dark:text-white"><?php echo $this->e($adminUsers); ?></div>
                    <div class="text-xs font-semibold text-slate-500 dark:text-[#9ca8ba] uppercase tracking-widest mt-2"><?php echo $this->e($trAdminUsers); ?></div>
                </div>
                <div class="w-12 h-12 bg-blue-50 dark:bg-blue-500/10 rounded-xl flex items-center justify-center">
                    <span class="material-symbols-outlined text-2xl text-blue-500">admin_panel_settings</span>
                </div>
            </div>
        </div>

        <!-- Reseller Users Card -->
        <div class="bg-white dark:bg-[#1a222f] p-6 rounded-xl border border-slate-200 dark:border-slate-800 hover:border-primary/40 transition-all shadow-sm lg:col-span-2">
            <div class="flex items-center justify-between">
                <div>
                    <div class="text-3xl font-black text-slate-900 dark:text-white"><?php echo $this->e($resellerUsers); ?></div>
                    <div class="text-xs font-semibold text-slate-500 dark:text-[#9ca8ba] uppercase tracking-widest mt-2"><?php echo $this->e($trResellerUsers); ?></div>
                </div>
                <div class="w-12 h-12 bg-emerald-50 dark:bg-emerald-500/10 rounded-xl flex items-center justify-center">
                    <span class="material-symbols-outlined text-2xl text-emerald-500">store</span>
                </div>
            </div>
        </div>

        <!-- Normal Users Card -->
        <div class="bg-white dark:bg-[#1a222f] p-6 rounded-xl border border-slate-200 dark:border-slate-800 hover:border-primary/40 transition-all shadow-sm lg:col-span-2">
            <div class="flex items-center justify-between">
                <div>
                    <div class="text-3xl font-black text-slate-900 dark:text-white"><?php echo $this->e($normalUsers); ?></div>
                    <div class="text-xs font-semibold text-slate-500 dark:text-[#9ca8ba] uppercase tracking-widest mt-2"><?php echo $this->e($trNormalUsers); ?></div>
                </div>
                <div class="w-12 h-12 bg-amber-50 dark:bg-amber-500/10 rounded-xl flex items-center justify-center">
                    <span class="material-symbols-outlined text-2xl text-amber-500">people</span>
                </div>
            </div>
        </div>

        <!-- Domains Card -->
        <div class="bg-white dark:bg-[#1a222f] p-6 rounded-xl border border-slate-200 dark:border-slate-800 hover:border-primary/40 transition-all shadow-sm lg:col-span-2">
            <div class="flex items-center justify-between">
                <div>
                    <div class="text-3xl font-black text-slate-900 dark:text-white"><?php echo $this->e($domains); ?></div>
                    <div class="text-xs font-semibold text-slate-500 dark:text-[#9ca8ba] uppercase tracking-widest mt-2"><?php echo $this->e($trDomains); ?></div>
                </div>
                <div class="w-12 h-12 bg-purple-50 dark:bg-purple-500/10 rounded-xl flex items-center justify-center">
                    <span class="material-symbols-outlined text-2xl text-purple-500">language</span>
                </div>
            </div>
        </div>

        <!-- FTP Accounts Card -->
        <div class="bg-white dark:bg-[#1a222f] p-6 rounded-xl border border-slate-200 dark:border-slate-800 hover:border-primary/40 transition-all shadow-sm lg:col-span-2">
            <div class="flex items-center justify-between">
                <div>
                    <div class="text-3xl font-black text-slate-900 dark:text-white"><?php echo $this->e($ftpAccounts); ?></div>
                    <div class="text-xs font-semibold text-slate-500 dark:text-[#9ca8ba] uppercase tracking-widest mt-2"><?php echo $this->e($trFtpAccounts); ?></div>
                </div>
                <div class="w-12 h-12 bg-cyan-50 dark:bg-cyan-500/10 rounded-xl flex items-center justify-center">
                    <span class="material-symbols-outlined text-2xl text-cyan-500">folder_open</span>
                </div>
            </div>
        </div>

        <!-- SQL Databases Card -->
        <div class="bg-white dark:bg-[#1a222f] p-6 rounded-xl border border-slate-200 dark:border-slate-800 hover:border-primary/40 transition-all shadow-sm lg:col-span-2">
            <div class="flex items-center justify-between">
                <div>
                    <div class="text-3xl font-black text-slate-900 dark:text-white"><?php echo $this->e($sqlDatabases); ?></div>
                    <div class="text-xs font-semibold text-slate-500 dark:text-[#9ca8ba] uppercase tracking-widest mt-2"><?php echo $this->e($trSqlDatabases); ?></div>
                </div>
                <div class="w-12 h-12 bg-indigo-50 dark:bg-indigo-500/10 rounded-xl flex items-center justify-center">
                    <span class="material-symbols-outlined text-2xl text-indigo-500">database</span>
                </div>
            </div>
        </div>

        <!-- SQL Users Card -->
        <div class="bg-white dark:bg-[#1a222f] p-6 rounded-xl border border-slate-200 dark:border-slate-800 hover:border-primary/40 transition-all shadow-sm lg:col-span-2">
            <div class="flex items-center justify-between">
                <div>
                    <div class="text-3xl font-black text-slate-900 dark:text-white"><?php echo $this->e($sqlUsers); ?></div>
                    <div class="text-xs font-semibold text-slate-500 dark:text-[#9ca8ba] uppercase tracking-widest mt-2"><?php echo $this->e($trSqlUsers); ?></div>
                </div>
                <div class="w-12 h-12 bg-orange-50 dark:bg-orange-500/10 rounded-xl flex items-center justify-center">
                    <span class="material-symbols-outlined text-2xl text-orange-500">manage_accounts</span>
                </div>
            </div>
        </div>

        <!-- Mail Accounts Card -->
        <div class="bg-white dark:bg-[#1a222f] p-6 rounded-xl border border-slate-200 dark:border-slate-800 hover:border-primary/40 transition-all shadow-sm lg:col-span-4">
            <div class="flex items-center justify-between">
                <div>
                    <div class="text-3xl font-black text-slate-900 dark:text-white"><?php echo $this->e($mailAccounts); ?></div>
                    <div class="text-xs font-semibold text-slate-500 dark:text-[#9ca8ba] uppercase tracking-widest mt-2"><?php echo $this->e($trMailAccounts); ?></div>
                </div>
                <div class="w-12 h-12 bg-red-50 dark:bg-red-500/10 rounded-xl flex items-center justify-center">
                    <span class="material-symbols-outlined text-2xl text-red-500">mail</span>
                </div>
            </div>
        </div>
    </div>

    <!-- Traffic Section -->
    <div class="bg-white dark:bg-[#1a222f] rounded-xl border border-slate-200 dark:border-slate-800 p-6 shadow-sm">
        <div class="flex items-center justify-between gap-6 mb-4">
            <h4 class="text-lg font-bold text-slate-900 dark:text-white flex items-center gap-2">
                <span class="material-symbols-outlined text-cyan-500">cloud_download</span>
                <?php echo $this->e($trServerTraffic); ?>
            </h4>
            <?php if (!empty($trafficWarning)): ?>
            <div class="text-sm text-slate-500 dark:text-[#9ca8ba]"><?php echo $this->e($trafficWarning); ?></div>
            <?php endif; ?>
        </div>
        <div class="h-6 bg-slate-200 dark:bg-slate-700 rounded-full overflow-hidden">
            <div class="h-full bg-gradient-to-r from-blue-500 to-cyan-500" style="width:<?php echo $trafficPercentWidth; ?>%"></div>
        </div>
        <div class="mt-2 text-right text-sm font-bold text-primary"><?php echo $trafficPercent; ?>%</div>
    </div>
</div>

