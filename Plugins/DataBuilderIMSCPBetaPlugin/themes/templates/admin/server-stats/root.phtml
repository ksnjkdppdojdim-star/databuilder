<?php
/**
 * Root template for admin server statistics page - Complete HTML structure
 * @var $block DataBuilder\Block\AbstractBlock
 */
?><!DOCTYPE html>
<html lang="en" class="dark h-full">
<head>
    <meta charset="UTF-8">
    <meta name="robots" content="nofollow, noindex">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Server Statistics - i-MSCP</title>
    <link rel="shortcut icon" href="/gui/themes/default/assets/images/favicon.ico">
    <link rel="stylesheet" href="/gui/themes/default/assets/css/theme.css">
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body.admin-layout {
            display: flex;
            height: 100vh;
            background: #f8fafc;
        }
        .admin-sidebar {
            width: 250px;
            background: white;
            border-right: 1px solid #e2e8f0;
            padding: 20px;
            overflow-y: auto;
        }
        .main-content-wrapper {
            flex: 1;
            display: flex;
            flex-direction: column;
            overflow: hidden;
        }
        .page-header {
            background: white;
            border-bottom: 1px solid #e2e8f0;
            padding: 20px 30px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        .main-content {
            flex: 1;
            overflow-y: auto;
            padding: 30px;
            background: #f8fafc;
        }
        .page-footer {
            background: white;
            border-top: 1px solid #e2e8f0;
            padding: 15px 30px;
            text-align: center;
            font-size: 12px;
            color: #666;
        }
    </style>
</head>
<body class="admin-layout">
    <?php echo $block->getChild('sidebar')->render(); ?>
    
    <div class="main-content-wrapper">
        <?php 
            $mainWrapper = $block->getChild('main_wrapper');
            if ($mainWrapper) {
                echo $mainWrapper->getChild('page_header')->render(); 
                echo $mainWrapper->getChild('content')->render(); 
                echo $mainWrapper->getChild('page_footer')->render(); 
            }
        ?>
    </div>
    
    <script src="/gui/themes/default/assets/js/jquery/jquery.js"></script>
    <script src="/gui/themes/default/assets/js/imscp.min.js"></script>
</body>
</html>
