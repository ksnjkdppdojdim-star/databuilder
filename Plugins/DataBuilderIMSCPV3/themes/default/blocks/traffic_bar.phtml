<?php
/**
 * DataBuilder Traffic Bar Block Template
 * 
 * A traffic visualization block with progress bar
 */

$title = $this->getParam('title', 'Server Traffic');
$warning = $this->getParam('warning', '');
$percentWidth = $this->getParam('percent_width', 0);
$percent = $this->getParam('percent', 0);
?>

<div class="bg-white dark:bg-[#1a222f] rounded-xl border border-slate-200 dark:border-slate-800 p-6 shadow-sm">
    <div class="flex items-center justify-between gap-6 mb-4">
        <h4 class="text-lg font-bold text-slate-900 dark:text-white flex items-center gap-2">
            <span class="material-symbols-outlined text-cyan-500">cloud_download</span>
            <?php echo $this->e($title); ?>
        </h4>
        <?php if (!empty($warning)): ?>
        <div class="text-sm text-slate-500 dark:text-[#9ca8ba]"><?php echo $this->e($warning); ?></div>
        <?php endif; ?>
    </div>
    <div class="h-6 bg-slate-200 dark:bg-slate-700 rounded-full overflow-hidden">
        <div class="h-full bg-gradient-to-r from-blue-500 to-cyan-500" style="width:<?php echo $percentWidth; ?>%"></div>
    </div>
    <div class="mt-2 text-right text-sm font-bold text-primary"><?php echo $percent; ?>%</div>
</div>

