<?php
/** @noinspection PhpUnhandledExceptionInspection */
?>

<div class="bg-blue-50 dark:bg-blue-900/20 border border-blue-200 dark:border-blue-800 rounded-lg p-4 mb-6">
    <div class="flex items-start">
        <span class="material-symbols-outlined text-blue-500 dark:text-blue-400 mr-3 mt-0.5">web</span>
        <div class="text-sm text-blue-700 dark:text-blue-300">
            <p class="mb-2">
                <?= tohtml(tr('Manage your DataBuilder pages. Create, edit, and delete pages with the visual page builder.'));?>
            </p>
        </div>
    </div>
</div>

<!-- Header with New Page button -->
<div class="flex justify-between items-center mb-6">
    <h3 class="text-xl font-bold text-slate-900 dark:text-slate-100">
        <?= tohtml(tr('Pages')) ?>
    </h3>
    <a href="/admin/databuilder/page/edit" 
       class="bg-primary hover:bg-primary/90 text-white px-4 py-2 rounded-lg font-semibold transition-all flex items-center">
        <span class="material-symbols-outlined mr-2">add</span>
        <?= tohtml(tr('New Page')) ?>
    </a>
</div>

<!-- Pages Table -->
<div class="bg-white dark:bg-slate-900 rounded-lg border border-neutral-border dark:border-slate-800 shadow-sm overflow-hidden">
    <table class="min-w-full divide-y divide-slate-200 dark:divide-slate-700">
        <thead class="bg-slate-50 dark:bg-slate-800">
            <tr>
                <th class="px-6 py-3 text-left text-xs font-medium text-slate-500 dark:text-slate-400 uppercase tracking-wider">
                    <?= tohtml(tr('Title')) ?>
                </th>
                <th class="px-6 py-3 text-left text-xs font-medium text-slate-500 dark:text-slate-400 uppercase tracking-wider">
                    <?= tohtml(tr('URL Key')) ?>
                </th>
                <th class="px-6 py-3 text-left text-xs font-medium text-slate-500 dark:text-slate-400 uppercase tracking-wider">
                    <?= tohtml(tr('Layout')) ?>
                </th>
                <th class="px-6 py-3 text-left text-xs font-medium text-slate-500 dark:text-slate-400 uppercase tracking-wider">
                    <?= tohtml(tr('Status')) ?>
                </th>
                <th class="px-6 py-3 text-left text-xs font-medium text-slate-500 dark:text-slate-400 uppercase tracking-wider">
                    <?= tohtml(tr('Actions')) ?>
                </th>
            </tr>
        </thead>
        <tbody class="bg-white dark:bg-slate-900 divide-y divide-slate-200 dark:divide-slate-700">
            {PAGE_ROWS}
            <tr id="no-pages-row" style="display: none;">
                <td colspan="5" class="px-6 py-8 text-center text-slate-500 dark:text-slate-400">
                    <?= tohtml(tr('No pages created yet. Click "New Page" to create your first page.')) ?>
                </td>
            </tr>
        </tbody>
    </table>
</div>

<script>
document.addEventListener('DOMContentLoaded', function() {
    const pageRows = document.querySelectorAll('tbody tr');
    const noPagesRow = document.getElementById('no-pages-row');
    
    if (pageRows.length === 0 && noPagesRow) {
        noPagesRow.style.display = 'table-row';
    }
});
</script>
